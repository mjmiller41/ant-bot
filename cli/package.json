{
  "name": "@antbot/cli",
  "version": "0.2.3",
  "type": "module",
  "bin": {
    "antbot": "./dist/index.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json && chmod +x dist/index.js",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@antbot/daemon": "workspace:*",
    "@antbot/contract": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^24.7.0",
    "typescript": "^5.9.3",
    "vitest": "^3.2.4"
  }
}
